import {chromium} from 'playwright';
import assert from 'node:assert/strict';
const browser=await chromium.launch();try{const page=await browser.newPage({viewport:{width:1600,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('file:///Users/test/hermes/flydesk-v2/DeFly_Colony_PitchDeck.html');await page.evaluate(()=>document.fonts.ready);await page.keyboard.press('Home');for(let i=0;i<10;i++){assert.equal(await page.locator('.slide.active').count(),1);assert.equal(await page.locator('#counter').textContent(),String(i+1).padStart(2,'0')+' / 10');const overflow=await page.locator('.slide.active').evaluate(s=>s.scrollHeight>s.clientHeight);assert.equal(overflow,false,'overflow slide '+(i+1));if([0,2,5,8].includes(i))await page.screenshot({path:'/tmp/defly-deck-'+i+'.png'});await page.keyboard.press('ArrowRight');}await page.keyboard.press('End');await page.reload();assert.equal(await page.locator('#counter').textContent(),'10 / 10');await page.setViewportSize({width:390,height:844});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);console.log(JSON.stringify({slides:10,keyboard:true,persistence:true,mobile:true,errors}));}finally{await browser.close();}
