import {chromium} from 'playwright';import assert from 'node:assert/strict';
const b=await chromium.launch();try{const p=await b.newPage({viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(process.env.DEFLY_TEST_URL||'http://127.0.0.1:8876/');await p.waitForFunction(()=>document.querySelector('#spikes').textContent.trim()!=='—',{},{timeout:120000});const r=await p.evaluate(()=>({spikes:document.querySelector('#spikes').textContent,time:document.querySelector('#brainTime').textContent,truth:document.querySelector('#neuralTruth').textContent,reason:document.querySelector('#neuralTruth').dataset.reason,overflow:document.documentElement.scrollWidth>innerWidth}));assert.equal(errors.length,0,errors.join('\n'));assert.match(r.truth,/BROWSER/);assert.equal(r.overflow,false);console.log(JSON.stringify({...r,errors}));await p.screenshot({path:'/tmp/defly-full-browser-live.png',fullPage:true});}finally{await b.close();}
