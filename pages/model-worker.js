self.onmessage=(e)=>{if(e.data?.type!=='observe')return;const s=e.data.series||[];const fast=s.slice(-5).reduce((a,b)=>a+b,0)/Math.max(1,s.slice(-5).length);const slow=s.slice(-20).reduce((a,b)=>a+b,0)/Math.max(1,s.slice(-20).length);const edge=(fast-slow)/(Math.abs(slow)||1);const action=edge>.004?'BUY':edge<-.004?'SELL':'HOLD';self.postMessage({action,confidence:Math.min(.88,.5+Math.abs(edge)*20),backend:'browser_market_only_debug'});};