import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),w=readFileSync(new URL('../worker/src/index.mjs',import.meta.url),'utf8');test('HOLD is penalized only when flat and escalates into a directional entry',()=>{assert.match(w,/proposal\.action\s*===\s*["']HOLD["']\s*&&\s*side\s*===\s*["']FLAT["']/);assert.match(w,/forcedAction\s*=\s*f\[0\]\s*>=\s*0\s*\?\s*["']BUY["']\s*:\s*["']SELL["']/);assert.match(w,/position_side/)});test('autonomy exposes proposed and executed actions',()=>{assert.match(w,/proposed_action/);assert.match(w,/executed_action/)});