import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const a=readFileSync(new URL('../pages/app.js',import.meta.url),'utf8');test('positioned HOLD never displays awaiting first signal',()=>{assert.match(a,/side\s*===\s*['"]FLAT['"]/);assert.match(a,/`HOLD \$\{side\}`/);assert.doesNotMatch(a,/executed_action\s*===\s*['"]HOLD['"]\s*&&\s*Number\(\s*d\.portfolio\?\.quantity/)});