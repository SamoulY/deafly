import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),h=readFileSync(new URL('pages/index.html',root),'utf8'),c=readFileSync(new URL('pages/app.js',root),'utf8'),css=readFileSync(new URL('pages/style.css',root),'utf8');test('chart contains only market plotting; actions are real DOM buttons',()=>{assert.doesNotMatch(h,/action-legend/);assert.doesNotMatch(c,/drawActionLegend/);const ui=readFileSync(new URL('pages/raising-ui.js',root),'utf8');for(const action of ['BUY','SELL','HOLD','CLOSE'])assert.ok(ui.includes(`data-teach="${action}"`));assert.match(c,/getContext\("2d"\)/)});