import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),html=readFileSync(new URL('pages/index.html',root),'utf8'),app=readFileSync(new URL('pages/app.js',root),'utf8');test('empty account does not claim HOLD BTC as an executed signal',()=>{assert.doesNotMatch(html,/HOLD BTC/);assert.match(html,/NO POSITION|AWAITING FIRST SIGNAL/);assert.match(app,/NO POSITION|AWAITING FIRST SIGNAL/)});