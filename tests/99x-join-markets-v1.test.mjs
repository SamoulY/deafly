import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),html=readFileSync(new URL('pages/index.html',root),'utf8'),worker=readFileSync(new URL('worker/src/index.mjs',root),'utf8');test('branding and explicit opt-in 99x join flow exist',()=>{assert.match(html,/99x\.meme Labs/);assert.match(html,/open99x/);assert.match(html,/close99x/);assert.match(html,/I agree/)});test('DeFly exposes the seven 99x market symbols',()=>{for(const s of['PEPEUSDT','DOGEUSDT','SHIBUSDT','SOLUSDT','ASTERUSDT','CAKEUSDT','UNIUSDT'])assert.ok(worker.includes(s),s)});