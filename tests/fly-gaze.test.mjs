import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),s=readFileSync(new URL('pages/scene.js',root),'utf8');test('fly and monitor face each other on the X axis',()=>{assert.ok(s.includes('fly.rotation.y = Math.PI / 2'));assert.ok(s.includes('monitor.rotation.y = -Math.PI / 2'));assert.ok(s.includes('camera.position.set(-6'))});test('monitor housing is perpendicular to desk viewer rather than side-on to fly',()=>{assert.match(s,/box\(root,\s*0\.24,\s*3\.1,\s*4\.8/)});