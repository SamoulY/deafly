import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const app=readFileSync(new URL('../pages/app.js',import.meta.url),'utf8');test('chart renders visible BUY and SELL markers',()=>{assert.match(app,/tradeMarkers/);assert.match(app,/BUY/);assert.match(app,/buy\s*\?\s*["']B["']\s*:\s*["']S["']/)});test('executed actions are added to chart markers',()=>{assert.match(app,/tradeMarkers\.push/);assert.match(app,/d\.executed_action/);assert.match(app,/d\.server_price|d\.market_price/)});