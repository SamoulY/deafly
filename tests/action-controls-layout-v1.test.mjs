import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),h=readFileSync(new URL('pages/index.html',root),'utf8'),c=readFileSync(new URL('pages/style.css',root),'utf8');test('external action strip is removed',()=>{assert.doesNotMatch(h,/class="actions action-legend"/)});test('human correction offers all four directional actions',()=>{for(const x of['data-correct="BUY"','data-correct="SELL"','data-correct="HOLD"','data-correct="CLOSE"'])assert.ok(h.includes(x),x);for(const x of['SHOULD OPEN LONG','SHOULD OPEN SHORT','SHOULD HOLD','SHOULD CLOSE'])assert.ok(h.includes(x),x)});