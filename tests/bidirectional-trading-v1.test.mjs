import test from'node:test';import assert from'node:assert/strict';import{chooseAction,defaultPolicy}from'../worker/src/learning.mjs';import{openLong,openShort,closePosition,checkRiskExit}from'../worker/src/core.mjs';test('policy has BUY SELL HOLD CLOSE actions',()=>{const p=defaultPolicy();assert.deepEqual(p.actions,['BUY','SELL','HOLD','CLOSE']);assert.equal(chooseAction(p,[0,0,1]).action,'HOLD')});test('BUY opens long and SELL opens short',()=>{const l=openLong({cash:10000,long_qty:0,short_qty:0},100);assert.ok(l.long_qty>0);const s=openShort({cash:10000,long_qty:0,short_qty:0},100);assert.ok(s.short_qty>0)});test('CLOSE removes either directional position',()=>{assert.equal(closePosition({cash:9000,long_qty:10,short_qty:0,long_entry:100,short_entry:0},110).long_qty,0);assert.equal(closePosition({cash:9000,long_qty:0,short_qty:10,long_entry:0,short_entry:100},90).short_qty,0)});test('fly-selected risk exit only fires on explicit model decision',()=>{assert.equal(checkRiskExit({side:'long',entry:100,mark:102,tp:0.01,sl:0.01,decision:'HOLD'}),null);assert.equal(checkRiskExit({side:'long',entry:100,mark:102,tp:0.01,sl:0.01,decision:'CLOSE'}),'TAKE_PROFIT')});