import test from'node:test';import assert from'node:assert/strict';import{buyFill,sellFill}from'../worker/src/core.mjs';test('fills accept D1 snake_case avg_cost',()=>{const b=buyFill({cash:10000,quantity:0,avg_cost:0},60000);assert.ok(Number.isFinite(b.avgCost));assert.ok(b.avgCost>0);const s=sellFill({cash:b.cash,quantity:b.quantity,avg_cost:b.avgCost},60000);assert.ok(Number.isFinite(s.avgCost));assert.equal(s.avgCost,0)});