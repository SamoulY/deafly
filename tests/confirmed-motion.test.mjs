import test from 'node:test';import assert from 'node:assert/strict';import {createRaisingController} from '../pages/raising-ui.js';
test('leg events follow accepted teaching results only, not load or failed orders',async()=>{const events=[];let fail=false;const s={id:'round',step:0,status:'ACTIVE'};const ui=createRaisingController({onAction:(...e)=>events.push(e),api:async(path)=>{if(path.endsWith('/profile'))return {profile:{loadout:{}},active_session_id:'round'};if(path.endsWith('/catalog'))return {items:[]};if(path.endsWith('/actions')){if(fail)throw Error('network');return {...s,step:1};}return s;}});await ui.load();assert.deepEqual(events,[]);fail=true;await assert.rejects(ui.act('BUY'));assert.deepEqual(events,[]);fail=false;await ui.act('BUY');assert.deepEqual(events,[['BUY','teaching:round:1']]);});
