import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),scene=readFileSync(new URL('pages/scene.js',root),'utf8'),html=readFileSync(new URL('pages/index.html',root),'utf8');test('scene exports deterministic non-black canvas probe markers',()=>{assert.match(scene,/data-lit-frame/);assert.match(scene,/renderer\.render\(scene,\s*camera\)/)});test('brand is DeFly everywhere in public HTML',()=>{assert.match(html,/DE<span>FLY<\/span>/);assert.match(html,/DEFLY V2 TRIAL/);assert.doesNotMatch(html,/FLYDESK/)});