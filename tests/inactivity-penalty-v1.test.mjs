import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const root=new URL('../',import.meta.url),w=readFileSync(new URL('worker/src/index.mjs',root),'utf8'),s=readFileSync(new URL('worker/schema.sql',root),'utf8'),a=readFileSync(new URL('pages/app.js',root),'utf8');test('every autonomous 10-second slot is claimed once',()=>{assert.match(s,/autonomy_step_claims/);assert.match(s,/UNIQUE\s*\(run_id,\s*slot\)/);assert.match(w,/Math\.floor\(\s*now\(\)\s*\/\s*10\s*\)/);assert.match(w,/AUTONOMY_SLOT_ALREADY_PROCESSED/)});test('flat HOLD is penalized every processed slot',()=>{assert.match(w,/INACTIVITY_PENALTY/);assert.match(w,/proposal\.action\s*===\s*["']HOLD["']\s*&&\s*side\s*===\s*["']FLAT["']/);assert.match(w,/applyPlasticity/)});test('browser cadence is ten seconds and autonomous status is explicit',()=>{assert.match(a,/setInterval\(\s*refreshObservation\s*,\s*10000\s*,?\s*\)/);assert.match(a,/NEXT STEP IN 30S/);});