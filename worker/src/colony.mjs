export const ACTIONS=['BUY','SELL','HOLD','CLOSE'];
export function vote(submissions,{quorum=0.5}={}){if(!Array.isArray(submissions)||!submissions.length)throw Error('NO_SUBMISSIONS');const counts=Object.fromEntries(ACTIONS.map(a=>[a,0]));for(const s of submissions){if(!ACTIONS.includes(s.action))throw Error('INVALID_ACTION');counts[s.action]++;}const total=submissions.length;let action='HOLD';if(counts.HOLD/total>=quorum)action='HOLD';else if(counts.BUY>counts.SELL)action='BUY';else if(counts.SELL>counts.BUY)action='SELL';const agreement=counts[action]/total;const distinct=new Set(submissions.map(s=>s.checkpoint_hash)).size;return {action,counts,total,agreement_rate:agreement,checkpoint_diversity:distinct,rule_version:'colony-vote-v1'};}
export function resultHash(result){return crypto.subtle.digest('SHA-256',new TextEncoder().encode(JSON.stringify(result))).then(b=>[...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join(''));}
